#!/usr/bin/env bash

# look for init.r or init.R or run.R
if [ -f run.R ]; then
    echo "R Shiny" && exit 0
elif [ -f init.r ] || [ -f init.R ]; then
    echo "R Console" && exit 0
else
    echo "None" && exit 1
fi


exit 0
